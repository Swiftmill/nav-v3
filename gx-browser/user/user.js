// Exemple : changer dynamiquement de thème depuis user.js
if (window?.gx?.storage) {
  console.info('[user.js] GX API disponible');
  // window.gx.storage.set('settings', { ... }) peut être utilisé ici avec prudence.
}
